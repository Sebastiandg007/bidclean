/**
 * RadiusExpansionProcessor unit tests.
 */
describe('RadiusExpansionProcessor', () => {
  describe('process', () => {
    it.todo('should skip stale jobs silently');
    it.todo('should expand radius by one step');
    it.todo('should discover new Cleaners within expanded radius');
    it.todo('should exclude already-delivered Cleaners');
    it.todo('should enqueue next expansion job');
    it.todo('should enqueue final-wait job at max radius');
    it.todo('should expire offer after final wait');
  });
});
