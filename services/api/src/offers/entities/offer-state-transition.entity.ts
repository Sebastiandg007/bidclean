/**
 * Offer state transition entity.
 *
 * Maps to the `offer_state_transitions` audit table.
 * Records every state change with timestamp, actor, and metadata.
 * Used for the state timeline display and debugging.
 */
export class OfferStateTransition {
  // TODO: Add TypeORM decorators in Task 6
}
